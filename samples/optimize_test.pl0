program optimize_test;
var x, y, z;
begin
    x := 10 + 5;
    y := x * 0;
    z := x + 0;
    if 1 < 2 then
        write(1)
    else
        write(0);
    
    if 1 > 2 then
        write(0)
    else
        write(1);

    while 1 > 2 do
        write(0);
    
    write(x, y, z)
end.
